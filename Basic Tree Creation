C++ Code
#include<bits/stdc++.h>
using namespace std;
struct TreeNode
{
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val)
    {
        data=val;
        left=nullptr;
        right=nullptr;
    }
};
int main()
{
    TreeNode* root=new TreeNode(1);
    root->left=new TreeNode(2);
    root->right=new TreeNode(3);
    root->left->left=new TreeNode(4);
    root->left->right=new TreeNode(5);
    cout<<"Binary Tree created successfully"<<endl;
}
output:
Binary Tree created successfully

-->With user input:
#include<bits/stdc++.h>
using namespace std;
struct TreeNode
{
    int data;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int val)
    {
        data=val;
        left=nullptr;
        right=nullptr;
    }

};
// Recursive function
TreeNode* createTree()
{
    int val;
    cout<<"Enter node value (-1 for no node):"<<endl;
    cin>>val;
    // Base case
    if(val==-1)
    {
        return nullptr;
    }
    TreeNode* root=new TreeNode(val);
    cout<<"Enter left child:"<<val<<endl;
    root->left=createTree();
    cout<<"Enter right child:"<<val<<endl;
    root->right=createTree();
    return root;
}
int main()
{
    cout<<"Enter root value(-1 for no tree)"<<endl;
    TreeNode* root=createTree();
    if(root==nullptr)
    {
        cout<<"Empty Tree"<<endl;
    }
    else
    {
        cout<<"Binary tree successfully created"<<endl;
    }
    return 0;
}

Input:
1
2
4
-1
-1
-1
3
-1
-1
Output:
Enter root value(-1 for no tree)
Enter node value (-1 for no node):
Enter left child:1
Enter node value (-1 for no node):
Enter left child:2
Enter node value (-1 for no node):
Enter left child:4
Enter node value (-1 for no node):
Enter right child:4
Enter node value (-1 for no node):
Enter right child:2
Enter node value (-1 for no node):
Enter right child:1
Enter node value (-1 for no node):
Enter left child:3
Enter node value (-1 for no node):
Enter right child:3
Enter node value (-1 for no node):
Binary tree successfully created
